/**
 * Implementa il Design Pattern Observer (GoF): {@code Subject} e {@code Observer} permettono a
 * un {@code Ride} di notificare i passeggeri iscritti quando il proprio stato cambia (nuova
 * prenotazione, posto liberato).
 */
package com.ispw.uniride.model.observer;
