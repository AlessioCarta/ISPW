/**
 * Contiene le Entity che rappresentano il dominio applicativo: {@code Student} (l'attore
 * studente), {@code Ride} (il passaggio offerto) e {@code Booking} (lo stato di approvazione di
 * una singola richiesta di prenotazione), insieme ai sotto-package {@code model.state},
 * {@code model.strategy} e {@code model.observer} che ne modellano il comportamento dinamico.
 */
package com.ispw.uniride.model;
